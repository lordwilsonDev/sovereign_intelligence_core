from __future__ import annotations

import uuid
from typing import Any, Dict

from msb_v2.aura.models import Event, EventPhase, State, Task
from msb_v2.aura.persistence import Persistence
from msb_v2.aura.runtime import log_event, now_ms, update_state
from msb_v2.aura.toolbelt import Toolbelt
from msb_v2.aura.memory.retriever import BM25Retriever
from msb_v2.aura.policies.engine import PolicyEngine


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__decide_tool__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__decide_tool__mutmut)
def _decide_tool(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_orig(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_1(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = None
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_2(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.upper()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_3(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "XXskillXX" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_4(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "SKILL" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_5(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" not in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_6(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("XXlist_skillsXX", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_7(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("LIST_SKILLS", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_8(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "XXget_skillXX"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_9(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "GET_SKILL"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_10(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name not in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_11(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "XXtimeXX" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_12(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "TIME" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_13(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" not in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_14(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "XXget_timeXX" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_15(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "GET_TIME" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_16(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" not in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_17(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "XXget_timeXX"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_18(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "GET_TIME"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_19(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "XXget_timeXX" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_20(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "GET_TIME" in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_21(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" not in toolbelt.available():
        return "get_time"
    return "echo"


def x__decide_tool__mutmut_22(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "XXget_timeXX"
    return "echo"


def x__decide_tool__mutmut_23(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "GET_TIME"
    return "echo"


def x__decide_tool__mutmut_24(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "XXechoXX"


def x__decide_tool__mutmut_25(goal: str, toolbelt: Toolbelt) -> str:
    goal_lower = goal.lower()
    if "skill" in goal_lower:
        for name in ("list_skills", "get_skill"):
            if name in toolbelt.available():
                return name
    if "time" in goal_lower:
        if "get_time" in toolbelt.available():
            return "get_time"
    if "get_time" in toolbelt.available():
        return "get_time"
    return "ECHO"

mutants_x__decide_tool__mutmut['_mutmut_orig'] = x__decide_tool__mutmut_orig # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_1'] = x__decide_tool__mutmut_1 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_2'] = x__decide_tool__mutmut_2 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_3'] = x__decide_tool__mutmut_3 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_4'] = x__decide_tool__mutmut_4 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_5'] = x__decide_tool__mutmut_5 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_6'] = x__decide_tool__mutmut_6 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_7'] = x__decide_tool__mutmut_7 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_8'] = x__decide_tool__mutmut_8 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_9'] = x__decide_tool__mutmut_9 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_10'] = x__decide_tool__mutmut_10 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_11'] = x__decide_tool__mutmut_11 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_12'] = x__decide_tool__mutmut_12 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_13'] = x__decide_tool__mutmut_13 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_14'] = x__decide_tool__mutmut_14 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_15'] = x__decide_tool__mutmut_15 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_16'] = x__decide_tool__mutmut_16 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_17'] = x__decide_tool__mutmut_17 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_18'] = x__decide_tool__mutmut_18 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_19'] = x__decide_tool__mutmut_19 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_20'] = x__decide_tool__mutmut_20 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_21'] = x__decide_tool__mutmut_21 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_22'] = x__decide_tool__mutmut_22 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_23'] = x__decide_tool__mutmut_23 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_24'] = x__decide_tool__mutmut_24 # type: ignore # mutmut generated
mutants_x__decide_tool__mutmut['x__decide_tool__mutmut_25'] = x__decide_tool__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAURACoreǁrun__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAURACoreǁ_phase__mutmut: MutantDict = {}  # type: ignore


class AURACore:
    @_mutmut_mutated(mutants_xǁAURACoreǁ__init____mutmut)
    def __init__(self, persistence: Persistence | None = None, toolbelt: Toolbelt | None = None) -> None:
        self.persistence = persistence or Persistence()
        self.toolbelt = toolbelt or Toolbelt()
    def xǁAURACoreǁ__init____mutmut_orig(self, persistence: Persistence | None = None, toolbelt: Toolbelt | None = None) -> None:
        self.persistence = persistence or Persistence()
        self.toolbelt = toolbelt or Toolbelt()
    def xǁAURACoreǁ__init____mutmut_1(self, persistence: Persistence | None = None, toolbelt: Toolbelt | None = None) -> None:
        self.persistence = None
        self.toolbelt = toolbelt or Toolbelt()
    def xǁAURACoreǁ__init____mutmut_2(self, persistence: Persistence | None = None, toolbelt: Toolbelt | None = None) -> None:
        self.persistence = persistence and Persistence()
        self.toolbelt = toolbelt or Toolbelt()
    def xǁAURACoreǁ__init____mutmut_3(self, persistence: Persistence | None = None, toolbelt: Toolbelt | None = None) -> None:
        self.persistence = persistence or Persistence()
        self.toolbelt = None
    def xǁAURACoreǁ__init____mutmut_4(self, persistence: Persistence | None = None, toolbelt: Toolbelt | None = None) -> None:
        self.persistence = persistence or Persistence()
        self.toolbelt = toolbelt and Toolbelt()

    @_mutmut_mutated(mutants_xǁAURACoreǁrun__mutmut)
    async def run(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_orig(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_1(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = None
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_2(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=None, current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_3(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=None)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_4(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_5(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), )
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_6(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id and str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_7(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(None), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_8(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = None
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_9(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["XX_start_msXX"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_10(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_START_MS"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_11(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = None
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_12(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(None, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_13(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, None, goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_14(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=None)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_15(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state("PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_16(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_17(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", )
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_18(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "XXPERCEIVEXX", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_19(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "perceive", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_20(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(None, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_21(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, None, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_22(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=None)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_23(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_24(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_25(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, )

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_26(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = None
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_27(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever and BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_28(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever(None)
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_29(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = None

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_30(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine and PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_31(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(None, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_32(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, None, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_33(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action=None, message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_34(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message=None)
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_35(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_36(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_37(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_38(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", )
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_39(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="XXnoopXX", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_40(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="NOOP", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_41(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="XXno memory retrievedXX")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_42(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="NO MEMORY RETRIEVED")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_43(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = None
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_44(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(None, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_45(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=None)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_46(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_47(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, )
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_48(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=4)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_49(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = None
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_50(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(None) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_51(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(None, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_52(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, None, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_53(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action=None, message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_54(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=None, memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_55(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=None)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_56(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_57(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_58(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_59(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_60(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", )
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_61(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="XXmemoryXX", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_62(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="MEMORY", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_63(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(None, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_64(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, None, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_65(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action=None, status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_66(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status=None, message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_67(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=None)

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_68(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_69(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_70(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_71(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_72(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", )

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_73(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="XXmemoryXX", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_74(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="MEMORY", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_75(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="XXERRORXX", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_76(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="error", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_77(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(None))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_78(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = None
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_79(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            None,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_80(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            None,
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_81(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision=None,
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_82(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_83(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_84(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_85(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "XXDECIDEXX",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_86(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "decide",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_87(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "XXtoolXX": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_88(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "TOOL": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_89(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "XXechoXX"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_90(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "ECHO"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_91(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith(None)
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_92(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.upper().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_93(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("XXsayXX")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_94(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("SAY")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_95(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(None, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_96(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, None),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_97(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_98(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, ),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_99(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "XXargumentsXX": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_100(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "ARGUMENTS": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_101(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"XXmessageXX": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_102(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"MESSAGE": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_103(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = None
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_104(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get(None, {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_105(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", None)
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_106(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get({})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_107(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", )
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_108(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("XXdecisionXX", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_109(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("DECISION", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_110(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(None, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_111(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, None, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_112(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=None)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_113(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_114(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_115(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, )
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_116(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = None
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_117(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"XXtoolXX": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_118(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"TOOL": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_119(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get(None, "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_120(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", None), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_121(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_122(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", ), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_123(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("XXtoolXX", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_124(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("TOOL", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_125(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "XXechoXX"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_126(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "ECHO"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_127(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "XXargumentsXX": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_128(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "ARGUMENTS": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_129(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get(None, {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_130(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", None)}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_131(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get({})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_132(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", )}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_133(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("XXargumentsXX", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_134(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("ARGUMENTS", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_135(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(None, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_136(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, None, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_137(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_138(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_139(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, )
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_140(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = None

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_141(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(None, state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_142(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), None, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_143(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, None)

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_144(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_145(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_146(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, )

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_147(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get(None, "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_148(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", None), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_149(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_150(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", ), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_151(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("XXtoolXX", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_152(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("TOOL", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_153(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "XXechoXX"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_154(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "ECHO"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_155(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get(None, {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_156(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", None))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_157(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get({}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_158(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", ))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_159(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("XXargumentsXX", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_160(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("ARGUMENTS", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_161(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = None
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_162(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get(None)
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_163(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("XXevent_idXX")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_164(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("EVENT_ID")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_165(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = None
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_166(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(None, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_167(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=None)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_168(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_169(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, )
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_170(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=11)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_171(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = None
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_172(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next(None, None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_173(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next(None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_174(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), )
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_175(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get(None) == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_176(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("XXevent_idXX") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_177(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("EVENT_ID") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_178(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") != tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_179(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row or tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_180(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get(None) in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_181(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("XXstatusXX") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_182(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("STATUS") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_183(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") not in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_184(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"XXSUCCESSXX", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_185(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"success", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_186(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "XXERRORXX"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_187(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "error"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_188(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = None
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_189(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["XXlast_tool_event_idXX"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_190(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["LAST_TOOL_EVENT_ID"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_191(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = None

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_192(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["XXlast_tool_statusXX"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_193(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["LAST_TOOL_STATUS"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_194(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get(None, "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_195(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", None)

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_196(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_197(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", )

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_198(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("XXstatusXX", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_199(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("STATUS", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_200(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "XXSUCCESSXX")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_201(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "success")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_202(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = None
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_203(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(None, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_204(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, None, result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_205(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=None)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_206(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state("REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_207(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_208(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", )
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_209(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "XXREFLECTXX", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_210(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "reflect", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_211(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = None
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_212(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"XXresultXX": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_213(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"RESULT": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_214(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = None
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_215(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["XXoutcomeXX"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_216(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["OUTCOME"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_217(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "XXsuccessXX" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_218(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "SUCCESS" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_219(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get(None) == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_220(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("XXstatusXX") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_221(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("STATUS") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_222(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") != "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_223(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "XXokXX" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_224(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "OK" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_225(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "XXfailureXX"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_226(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "FAILURE"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_227(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = None
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_228(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(None, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_229(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, None, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_230(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=None, payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_231(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=None)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_232(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_233(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_234(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_235(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), )
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_236(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get(None, "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_237(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", None), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_238(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_239(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", ), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_240(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("XXstatusXX", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_241(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("STATUS", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_242(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "XXFAILEDXX"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_243(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "failed"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_244(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = None
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_245(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(None)
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_246(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get(None, now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_247(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", None))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_248(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get(now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_249(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", ))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_250(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("XX_start_msXX", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_251(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_START_MS", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_252(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = None
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_253(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() + start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_254(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(None)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_255(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append(None)

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_256(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault(None, []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_257(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", None).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_258(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault([]).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_259(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", ).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_260(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("XXphase_eventsXX", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_261(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("PHASE_EVENTS", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_262(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"XXphaseXX": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_263(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"PHASE": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_264(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "XXevent_idXX": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_265(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "EVENT_ID": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_266(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = None
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_267(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=None, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_268(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata=None)
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_269(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_270(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, )
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_271(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"XXsession_idXX": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_272(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"SESSION_ID": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_273(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "XXstep_countXX": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_274(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "STEP_COUNT": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_275(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(None)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_276(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = None
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_277(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = None
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_278(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["XXtask_statusXX"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_279(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["TASK_STATUS"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_280(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get(None) != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_281(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("XXstatusXX") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_282(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("STATUS") != "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_283(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") == "ok":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_284(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "XXokXX":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_285(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "OK":
            raise RuntimeError(tool_result.get("message", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_286(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(None)
        return state

    async def xǁAURACoreǁrun__mutmut_287(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get(None, "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_288(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", None))
        return state

    async def xǁAURACoreǁrun__mutmut_289(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_290(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", ))
        return state

    async def xǁAURACoreǁrun__mutmut_291(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("XXmessageXX", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_292(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("MESSAGE", "tool failed"))
        return state

    async def xǁAURACoreǁrun__mutmut_293(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "XXtool failedXX"))
        return state

    async def xǁAURACoreǁrun__mutmut_294(
        self,
        goal: str,
        session_id: str | None = None,
        retriever: Any | None = None,
        policy_engine: Any | None = None,
    ) -> State:
        state = State(session_id=session_id or str(uuid.uuid4().hex), current_goal=goal)
        state.context["_start_ms"] = now_ms()
        state = update_state(state, "PERCEIVE", goal=goal)
        await self._phase(state, EventPhase.PERCEIVE, goal=goal)

        retriever = retriever or BM25Retriever([])
        policy_engine = policy_engine or PolicyEngine()

        await self._phase(state, EventPhase.ORIENT, action="noop", message="no memory retrieved")
        try:
            memories = await retriever.retrieve(goal, top_k=3)
            redacted = [policy_engine.apply(m) for m in memories]
            await self._phase(state, EventPhase.ORIENT, action="memory", message=f"retrieved {len(redacted)} memories", memories=redacted)
        except Exception as exc:
            await self._phase(state, EventPhase.ORIENT, action="memory", status="ERROR", message=str(exc))

        state = update_state(
            state,
            "DECIDE",
            decision={
                "tool": "echo"
                if goal.lower().startswith("say")
                else _decide_tool(goal, self.toolbelt),
                "arguments": {"message": goal},
            },
        )
        decision = state.context.get("decision", {})
        await self._phase(state, EventPhase.DECIDE, decision=decision)
        act_payload = {"tool": decision.get("tool", "echo"), "arguments": decision.get("arguments", {})}
        await self._phase(state, EventPhase.ACT, **act_payload)
        tool_result = await self.toolbelt.call(decision.get("tool", "echo"), state, decision.get("arguments", {}))

        tool_event_id = tool_result.get("event_id")
        if tool_event_id:
            tool_rows = self.persistence.recent_events(state.session_id, limit=10)
            tool_row = next((r for r in tool_rows if r.get("event_id") == tool_event_id), None)
            if tool_row and tool_row.get("status") in {"SUCCESS", "ERROR"}:
                state.context["last_tool_event_id"] = tool_event_id
                state.context["last_tool_status"] = tool_row.get("status", "SUCCESS")

        state = update_state(state, "REFLECT", result=tool_result)
        reflect_payload: Dict[str, Any] = {"result": tool_result}
        reflect_payload["outcome"] = "success" if tool_result.get("status") == "ok" else "failure"
        evt = log_event(state, EventPhase.REFLECT, status=tool_result.get("status", "FAILED"), payload=reflect_payload)
        start_ms = float(state.context.get("_start_ms", now_ms()))
        evt.latency_ms = now_ms() - start_ms
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": EventPhase.REFLECT.value, "event_id": evt.event_id})

        task = Task(goal=goal, metadata={"session_id": state.session_id, "step_count": state.step})
        self.persistence.save_task(task.__dict__)
        state.task_id = task.task_id
        state.context["task_status"] = task.status
        if tool_result.get("status") != "ok":
            raise RuntimeError(tool_result.get("message", "TOOL FAILED"))
        return state

    @_mutmut_mutated(mutants_xǁAURACoreǁ_phase__mutmut)
    async def _phase(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_orig(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_1(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = None
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_2(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(None, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_3(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, None, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_4(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=None)
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_5(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_6(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_7(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, )
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_8(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload and {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_9(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(None)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_10(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append(None)
        return evt

    async def xǁAURACoreǁ_phase__mutmut_11(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault(None, []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_12(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", None).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_13(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault([]).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_14(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", ).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_15(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("XXphase_eventsXX", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_16(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("PHASE_EVENTS", []).append({"phase": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_17(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"XXphaseXX": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_18(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"PHASE": phase.value, "event_id": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_19(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "XXevent_idXX": evt.event_id})
        return evt

    async def xǁAURACoreǁ_phase__mutmut_20(self, state: State, phase: EventPhase, **payload: Any) -> Event:
        evt = log_event(state, phase, payload=payload or {})
        self.persistence.save_event(evt.__dict__)
        state.context.setdefault("phase_events", []).append({"phase": phase.value, "EVENT_ID": evt.event_id})
        return evt

mutants_xǁAURACoreǁ__init____mutmut['_mutmut_orig'] = AURACore.xǁAURACoreǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAURACoreǁ__init____mutmut['xǁAURACoreǁ__init____mutmut_1'] = AURACore.xǁAURACoreǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ__init____mutmut['xǁAURACoreǁ__init____mutmut_2'] = AURACore.xǁAURACoreǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ__init____mutmut['xǁAURACoreǁ__init____mutmut_3'] = AURACore.xǁAURACoreǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ__init____mutmut['xǁAURACoreǁ__init____mutmut_4'] = AURACore.xǁAURACoreǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁAURACoreǁrun__mutmut['_mutmut_orig'] = AURACore.xǁAURACoreǁrun__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_1'] = AURACore.xǁAURACoreǁrun__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_2'] = AURACore.xǁAURACoreǁrun__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_3'] = AURACore.xǁAURACoreǁrun__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_4'] = AURACore.xǁAURACoreǁrun__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_5'] = AURACore.xǁAURACoreǁrun__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_6'] = AURACore.xǁAURACoreǁrun__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_7'] = AURACore.xǁAURACoreǁrun__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_8'] = AURACore.xǁAURACoreǁrun__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_9'] = AURACore.xǁAURACoreǁrun__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_10'] = AURACore.xǁAURACoreǁrun__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_11'] = AURACore.xǁAURACoreǁrun__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_12'] = AURACore.xǁAURACoreǁrun__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_13'] = AURACore.xǁAURACoreǁrun__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_14'] = AURACore.xǁAURACoreǁrun__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_15'] = AURACore.xǁAURACoreǁrun__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_16'] = AURACore.xǁAURACoreǁrun__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_17'] = AURACore.xǁAURACoreǁrun__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_18'] = AURACore.xǁAURACoreǁrun__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_19'] = AURACore.xǁAURACoreǁrun__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_20'] = AURACore.xǁAURACoreǁrun__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_21'] = AURACore.xǁAURACoreǁrun__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_22'] = AURACore.xǁAURACoreǁrun__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_23'] = AURACore.xǁAURACoreǁrun__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_24'] = AURACore.xǁAURACoreǁrun__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_25'] = AURACore.xǁAURACoreǁrun__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_26'] = AURACore.xǁAURACoreǁrun__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_27'] = AURACore.xǁAURACoreǁrun__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_28'] = AURACore.xǁAURACoreǁrun__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_29'] = AURACore.xǁAURACoreǁrun__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_30'] = AURACore.xǁAURACoreǁrun__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_31'] = AURACore.xǁAURACoreǁrun__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_32'] = AURACore.xǁAURACoreǁrun__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_33'] = AURACore.xǁAURACoreǁrun__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_34'] = AURACore.xǁAURACoreǁrun__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_35'] = AURACore.xǁAURACoreǁrun__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_36'] = AURACore.xǁAURACoreǁrun__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_37'] = AURACore.xǁAURACoreǁrun__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_38'] = AURACore.xǁAURACoreǁrun__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_39'] = AURACore.xǁAURACoreǁrun__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_40'] = AURACore.xǁAURACoreǁrun__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_41'] = AURACore.xǁAURACoreǁrun__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_42'] = AURACore.xǁAURACoreǁrun__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_43'] = AURACore.xǁAURACoreǁrun__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_44'] = AURACore.xǁAURACoreǁrun__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_45'] = AURACore.xǁAURACoreǁrun__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_46'] = AURACore.xǁAURACoreǁrun__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_47'] = AURACore.xǁAURACoreǁrun__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_48'] = AURACore.xǁAURACoreǁrun__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_49'] = AURACore.xǁAURACoreǁrun__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_50'] = AURACore.xǁAURACoreǁrun__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_51'] = AURACore.xǁAURACoreǁrun__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_52'] = AURACore.xǁAURACoreǁrun__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_53'] = AURACore.xǁAURACoreǁrun__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_54'] = AURACore.xǁAURACoreǁrun__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_55'] = AURACore.xǁAURACoreǁrun__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_56'] = AURACore.xǁAURACoreǁrun__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_57'] = AURACore.xǁAURACoreǁrun__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_58'] = AURACore.xǁAURACoreǁrun__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_59'] = AURACore.xǁAURACoreǁrun__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_60'] = AURACore.xǁAURACoreǁrun__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_61'] = AURACore.xǁAURACoreǁrun__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_62'] = AURACore.xǁAURACoreǁrun__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_63'] = AURACore.xǁAURACoreǁrun__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_64'] = AURACore.xǁAURACoreǁrun__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_65'] = AURACore.xǁAURACoreǁrun__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_66'] = AURACore.xǁAURACoreǁrun__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_67'] = AURACore.xǁAURACoreǁrun__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_68'] = AURACore.xǁAURACoreǁrun__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_69'] = AURACore.xǁAURACoreǁrun__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_70'] = AURACore.xǁAURACoreǁrun__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_71'] = AURACore.xǁAURACoreǁrun__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_72'] = AURACore.xǁAURACoreǁrun__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_73'] = AURACore.xǁAURACoreǁrun__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_74'] = AURACore.xǁAURACoreǁrun__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_75'] = AURACore.xǁAURACoreǁrun__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_76'] = AURACore.xǁAURACoreǁrun__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_77'] = AURACore.xǁAURACoreǁrun__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_78'] = AURACore.xǁAURACoreǁrun__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_79'] = AURACore.xǁAURACoreǁrun__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_80'] = AURACore.xǁAURACoreǁrun__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_81'] = AURACore.xǁAURACoreǁrun__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_82'] = AURACore.xǁAURACoreǁrun__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_83'] = AURACore.xǁAURACoreǁrun__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_84'] = AURACore.xǁAURACoreǁrun__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_85'] = AURACore.xǁAURACoreǁrun__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_86'] = AURACore.xǁAURACoreǁrun__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_87'] = AURACore.xǁAURACoreǁrun__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_88'] = AURACore.xǁAURACoreǁrun__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_89'] = AURACore.xǁAURACoreǁrun__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_90'] = AURACore.xǁAURACoreǁrun__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_91'] = AURACore.xǁAURACoreǁrun__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_92'] = AURACore.xǁAURACoreǁrun__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_93'] = AURACore.xǁAURACoreǁrun__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_94'] = AURACore.xǁAURACoreǁrun__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_95'] = AURACore.xǁAURACoreǁrun__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_96'] = AURACore.xǁAURACoreǁrun__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_97'] = AURACore.xǁAURACoreǁrun__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_98'] = AURACore.xǁAURACoreǁrun__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_99'] = AURACore.xǁAURACoreǁrun__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_100'] = AURACore.xǁAURACoreǁrun__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_101'] = AURACore.xǁAURACoreǁrun__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_102'] = AURACore.xǁAURACoreǁrun__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_103'] = AURACore.xǁAURACoreǁrun__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_104'] = AURACore.xǁAURACoreǁrun__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_105'] = AURACore.xǁAURACoreǁrun__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_106'] = AURACore.xǁAURACoreǁrun__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_107'] = AURACore.xǁAURACoreǁrun__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_108'] = AURACore.xǁAURACoreǁrun__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_109'] = AURACore.xǁAURACoreǁrun__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_110'] = AURACore.xǁAURACoreǁrun__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_111'] = AURACore.xǁAURACoreǁrun__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_112'] = AURACore.xǁAURACoreǁrun__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_113'] = AURACore.xǁAURACoreǁrun__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_114'] = AURACore.xǁAURACoreǁrun__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_115'] = AURACore.xǁAURACoreǁrun__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_116'] = AURACore.xǁAURACoreǁrun__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_117'] = AURACore.xǁAURACoreǁrun__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_118'] = AURACore.xǁAURACoreǁrun__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_119'] = AURACore.xǁAURACoreǁrun__mutmut_119 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_120'] = AURACore.xǁAURACoreǁrun__mutmut_120 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_121'] = AURACore.xǁAURACoreǁrun__mutmut_121 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_122'] = AURACore.xǁAURACoreǁrun__mutmut_122 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_123'] = AURACore.xǁAURACoreǁrun__mutmut_123 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_124'] = AURACore.xǁAURACoreǁrun__mutmut_124 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_125'] = AURACore.xǁAURACoreǁrun__mutmut_125 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_126'] = AURACore.xǁAURACoreǁrun__mutmut_126 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_127'] = AURACore.xǁAURACoreǁrun__mutmut_127 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_128'] = AURACore.xǁAURACoreǁrun__mutmut_128 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_129'] = AURACore.xǁAURACoreǁrun__mutmut_129 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_130'] = AURACore.xǁAURACoreǁrun__mutmut_130 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_131'] = AURACore.xǁAURACoreǁrun__mutmut_131 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_132'] = AURACore.xǁAURACoreǁrun__mutmut_132 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_133'] = AURACore.xǁAURACoreǁrun__mutmut_133 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_134'] = AURACore.xǁAURACoreǁrun__mutmut_134 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_135'] = AURACore.xǁAURACoreǁrun__mutmut_135 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_136'] = AURACore.xǁAURACoreǁrun__mutmut_136 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_137'] = AURACore.xǁAURACoreǁrun__mutmut_137 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_138'] = AURACore.xǁAURACoreǁrun__mutmut_138 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_139'] = AURACore.xǁAURACoreǁrun__mutmut_139 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_140'] = AURACore.xǁAURACoreǁrun__mutmut_140 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_141'] = AURACore.xǁAURACoreǁrun__mutmut_141 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_142'] = AURACore.xǁAURACoreǁrun__mutmut_142 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_143'] = AURACore.xǁAURACoreǁrun__mutmut_143 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_144'] = AURACore.xǁAURACoreǁrun__mutmut_144 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_145'] = AURACore.xǁAURACoreǁrun__mutmut_145 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_146'] = AURACore.xǁAURACoreǁrun__mutmut_146 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_147'] = AURACore.xǁAURACoreǁrun__mutmut_147 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_148'] = AURACore.xǁAURACoreǁrun__mutmut_148 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_149'] = AURACore.xǁAURACoreǁrun__mutmut_149 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_150'] = AURACore.xǁAURACoreǁrun__mutmut_150 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_151'] = AURACore.xǁAURACoreǁrun__mutmut_151 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_152'] = AURACore.xǁAURACoreǁrun__mutmut_152 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_153'] = AURACore.xǁAURACoreǁrun__mutmut_153 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_154'] = AURACore.xǁAURACoreǁrun__mutmut_154 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_155'] = AURACore.xǁAURACoreǁrun__mutmut_155 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_156'] = AURACore.xǁAURACoreǁrun__mutmut_156 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_157'] = AURACore.xǁAURACoreǁrun__mutmut_157 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_158'] = AURACore.xǁAURACoreǁrun__mutmut_158 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_159'] = AURACore.xǁAURACoreǁrun__mutmut_159 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_160'] = AURACore.xǁAURACoreǁrun__mutmut_160 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_161'] = AURACore.xǁAURACoreǁrun__mutmut_161 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_162'] = AURACore.xǁAURACoreǁrun__mutmut_162 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_163'] = AURACore.xǁAURACoreǁrun__mutmut_163 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_164'] = AURACore.xǁAURACoreǁrun__mutmut_164 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut
# type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_267'] = AURACore.xǁAURACoreǁrun__mutmut_267 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_268'] = AURACore.xǁAURACoreǁrun__mutmut_268 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_269'] = AURACore.xǁAURACoreǁrun__mutmut_269 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_270'] = AURACore.xǁAURACoreǁrun__mutmut_270 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_271'] = AURACore.xǁAURACoreǁrun__mutmut_271 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_272'] = AURACore.xǁAURACoreǁrun__mutmut_272 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_273'] = AURACore.xǁAURACoreǁrun__mutmut_273 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_274'] = AURACore.xǁAURACoreǁrun__mutmut_274 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_275'] = AURACore.xǁAURACoreǁrun__mutmut_275 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_276'] = AURACore.xǁAURACoreǁrun__mutmut_276 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_277'] = AURACore.xǁAURACoreǁrun__mutmut_277 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_278'] = AURACore.xǁAURACoreǁrun__mutmut_278 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_279'] = AURACore.xǁAURACoreǁrun__mutmut_279 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_280'] = AURACore.xǁAURACoreǁrun__mutmut_280 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_281'] = AURACore.xǁAURACoreǁrun__mutmut_281 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_282'] = AURACore.xǁAURACoreǁrun__mutmut_282 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_283'] = AURACore.xǁAURACoreǁrun__mutmut_283 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_284'] = AURACore.xǁAURACoreǁrun__mutmut_284 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_285'] = AURACore.xǁAURACoreǁrun__mutmut_285 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_286'] = AURACore.xǁAURACoreǁrun__mutmut_286 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_287'] = AURACore.xǁAURACoreǁrun__mutmut_287 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_288'] = AURACore.xǁAURACoreǁrun__mutmut_288 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_289'] = AURACore.xǁAURACoreǁrun__mutmut_289 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_290'] = AURACore.xǁAURACoreǁrun__mutmut_290 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_291'] = AURACore.xǁAURACoreǁrun__mutmut_291 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_292'] = AURACore.xǁAURACoreǁrun__mutmut_292 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_293'] = AURACore.xǁAURACoreǁrun__mutmut_293 # type: ignore # mutmut generated
mutants_xǁAURACoreǁrun__mutmut['xǁAURACoreǁrun__mutmut_294'] = AURACore.xǁAURACoreǁrun__mutmut_294 # type: ignore # mutmut generated

mutants_xǁAURACoreǁ_phase__mutmut['_mutmut_orig'] = AURACore.xǁAURACoreǁ_phase__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_1'] = AURACore.xǁAURACoreǁ_phase__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_2'] = AURACore.xǁAURACoreǁ_phase__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_3'] = AURACore.xǁAURACoreǁ_phase__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_4'] = AURACore.xǁAURACoreǁ_phase__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_5'] = AURACore.xǁAURACoreǁ_phase__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_6'] = AURACore.xǁAURACoreǁ_phase__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_7'] = AURACore.xǁAURACoreǁ_phase__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_8'] = AURACore.xǁAURACoreǁ_phase__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_9'] = AURACore.xǁAURACoreǁ_phase__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_10'] = AURACore.xǁAURACoreǁ_phase__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_11'] = AURACore.xǁAURACoreǁ_phase__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_12'] = AURACore.xǁAURACoreǁ_phase__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_13'] = AURACore.xǁAURACoreǁ_phase__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_14'] = AURACore.xǁAURACoreǁ_phase__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_15'] = AURACore.xǁAURACoreǁ_phase__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_16'] = AURACore.xǁAURACoreǁ_phase__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_17'] = AURACore.xǁAURACoreǁ_phase__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_18'] = AURACore.xǁAURACoreǁ_phase__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_19'] = AURACore.xǁAURACoreǁ_phase__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAURACoreǁ_phase__mutmut['xǁAURACoreǁ_phase__mutmut_20'] = AURACore.xǁAURACoreǁ_phase__mutmut_20 # type: ignore # mutmut generated
